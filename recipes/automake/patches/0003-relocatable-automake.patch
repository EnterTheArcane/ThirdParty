--- a/bin/aclocal.in
+++ b/bin/aclocal.in
@@ -22,7 +22,10 @@
 use 5.006; use strict; use warnings;
+use Cwd 'abs_path';
+use File::Basename;
 
 BEGIN
 {
-  unshift (@INC, '@datadir@/@PACKAGE@-@APIVERSION@')
+  my $scriptpath = abs_path(dirname(__FILE__));
+  unshift (@INC, "$scriptpath/../share/@PACKAGE@-@APIVERSION@")
     unless $ENV{AUTOMAKE_UNINSTALLED};
 }
@@ -63,6 +66,12 @@
 # --aclocal-path option/ACLOCAL_PATH environment variable, and reset
 # with the '--system-acdir' option.
+my $scriptpath = abs_path(dirname(__FILE__));
 my @user_includes = ();
-my @automake_includes = ('@datadir@/aclocal-' . $APIVERSION);
-my @system_includes = ('@datadir@/aclocal');
+my @automake_includes = ("$scriptpath/../share/aclocal-" . $APIVERSION);
+my @system_includes = ("$scriptpath/../share/aclocal");
+my @recipe_includes;
+foreach my $recipe_dir (uniq(split(/[:;]/, $ENV{'AUTOMAKE_RECIPE_INCLUDES'} || "")))
+{
+  push (@recipe_includes, $recipe_dir) if $recipe_dir ne '' && -d $recipe_dir;
+}
 my $aclocal_path = '';
@@ -448,4 +457,5 @@ sub scan_m4_files ()
   scan_m4_dirs (FT_AUTOMAKE, SCAN_M4_DIRS_ERROR, @automake_includes);
   scan_m4_dirs (FT_SYSTEM, SCAN_M4_DIRS_ERROR, @system_includes);
+  scan_m4_dirs (FT_SYSTEM, SCAN_M4_DIRS_ERROR, @recipe_includes);
 
   # Construct a new function that does the searching.  We use a
@@ -767,5 +777,5 @@ sub trace_used_macros ()
   # aclocal.m4 is not yet available.
   local $ENV{WARNINGS} = 'none';
 
-  my $traces = ($ENV{AUTOM4TE} || '@am_AUTOM4TE@');
+  my $traces = ($ENV{AUTOM4TE} || '/usr/bin/env autom4te');
   $traces .= " --language Autoconf-without-aclocal-m4 ";
--- a/bin/automake.in
+++ b/bin/automake.in
@@ -25,6 +25,9 @@ package Automake;
 use 5.006; use strict; use warnings;
+use Cwd 'abs_path';
+use File::Basename;
 
 BEGIN
 {
-  unshift (@INC, '@datadir@/@PACKAGE@-@APIVERSION@')
+  my $scriptpath = abs_path(dirname(__FILE__));
+  unshift (@INC, "$scriptpath/../share/@PACKAGE@-@APIVERSION@")
     unless $ENV{AUTOMAKE_UNINSTALLED};
@@ -5329,6 +5332,6 @@ sub scan_autoconf_traces
   # and will see its warnings then.
   local $ENV{WARNINGS} = 'none';
 
-  my $traces = ($ENV{AUTOCONF} || '@am_AUTOCONF@') . " ";
+  my $traces = ($ENV{AUTOCONF} || '/usr/bin/env autoconf') . " ";
 
   # Use a separator unlikely to be used, not ':', the default, which
--- a/lib/Automake/Config.in
+++ b/lib/Automake/Config.in
@@ -19,4 +19,6 @@ package Automake::Config;
 
 use 5.006; use strict; use warnings;
+use Cwd 'abs_path';
+use File::Basename;
 
 use Exporter;
@@ -33,5 +35,6 @@ our $PACKAGE_BUGREPORT = '@PACKAGE_BUGREPORT@';
 our $VERSION = '@VERSION@';
 our $RELEASE_YEAR = '@RELEASE_YEAR@';
-our $libdir = $ENV{"AUTOMAKE_LIBDIR"} || '@datadir@/@PACKAGE@-@APIVERSION@';
+my $scriptpath = abs_path(dirname(__FILE__));
+our $libdir = $ENV{"AUTOMAKE_LIBDIR"} || "$scriptpath/../../@PACKAGE@-@APIVERSION@";
 
 our $perl_threads = 0;
