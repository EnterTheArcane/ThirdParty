--- libpkgconf/path.c
+++ libpkgconf/path.c
@@ -211 +211 @@ pkgconf_path_split(const char *text, pkgconf_list_t *dirlist, bool filter)
-	while ((p = strtok(iter, PKG_CONFIG_PATH_SEP_S)) != NULL)
+	while ((p = strtok(iter, ";:")) != NULL)
